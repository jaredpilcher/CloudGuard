"""
LangGraph CloudGuard - Embedding-cloud guardrails & routing for LangGraph.

A pure plugin that assumes nothing about embeddings or LLMs. 
The host graph must inject those at runtime (local or API).
"""

__version__ = "0.1.0"